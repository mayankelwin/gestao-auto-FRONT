Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils = require("./utils.cjs");
const require_useQueryClient = require("./useQueryClient.cjs");
let vue_demi = require("vue-demi");
//#region src/useIsFetching.ts
function useIsFetching(fetchingFilters = {}, queryClient) {
	if (process.env.NODE_ENV === "development") {
		if (!(0, vue_demi.getCurrentScope)()) console.warn("vue-query composable like \"useQuery()\" should only be used inside a \"setup()\" function or a running effect scope. They might otherwise lead to memory leaks.");
	}
	const client = queryClient || require_useQueryClient.useQueryClient();
	const isFetching = (0, vue_demi.ref)();
	const listener = () => {
		const resolvedFilters = typeof fetchingFilters === "function" ? fetchingFilters() : fetchingFilters;
		isFetching.value = client.isFetching(require_utils.cloneDeepUnref(resolvedFilters));
	};
	const unsubscribe = client.getQueryCache().subscribe(listener);
	(0, vue_demi.watchEffect)(listener);
	(0, vue_demi.onScopeDispose)(() => {
		unsubscribe();
	});
	return isFetching;
}
//#endregion
exports.useIsFetching = useIsFetching;

//# sourceMappingURL=useIsFetching.cjs.map