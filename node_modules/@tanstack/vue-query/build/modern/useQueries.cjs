Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils = require("./utils.cjs");
const require_useQueryClient = require("./useQueryClient.cjs");
let _tanstack_query_core = require("@tanstack/query-core");
let vue_demi = require("vue-demi");
//#region src/useQueries.ts
function useQueries({ queries, ...options }, queryClient) {
	if (process.env.NODE_ENV === "development") {
		if (!(0, vue_demi.getCurrentScope)()) console.warn("vue-query composable like \"useQuery()\" should only be used inside a \"setup()\" function or a running effect scope. They might otherwise lead to memory leaks.");
	}
	const client = queryClient || require_useQueryClient.useQueryClient();
	const defaultedQueries = (0, vue_demi.computed)(() => {
		const resolvedQueries = typeof queries === "function" ? queries() : queries;
		return (0, vue_demi.unref)(resolvedQueries).map((queryOptions) => {
			const clonedOptions = require_utils.cloneDeepUnref(queryOptions);
			if (typeof clonedOptions.enabled === "function") clonedOptions.enabled = queryOptions.enabled();
			const defaulted = client.defaultQueryOptions(clonedOptions);
			defaulted._optimisticResults = client.isRestoring?.value ? "isRestoring" : "optimistic";
			return defaulted;
		});
	});
	const observer = new _tanstack_query_core.QueriesObserver(client, defaultedQueries.value, options);
	const getOptimisticResult = () => {
		const [results, getCombinedResult] = observer.getOptimisticResult(defaultedQueries.value, options.combine);
		return getCombinedResult(results.map((result, index) => {
			return {
				...result,
				refetch: async (...args) => {
					const [{ [index]: query }] = observer.getOptimisticResult(defaultedQueries.value, options.combine);
					return query.refetch(...args);
				}
			};
		}));
	};
	const state = (0, vue_demi.shallowRef)(getOptimisticResult());
	let unsubscribe = () => {};
	if (client.isRestoring) (0, vue_demi.watch)(client.isRestoring, (isRestoring) => {
		if (!isRestoring) {
			unsubscribe();
			unsubscribe = observer.subscribe(() => {
				state.value = getOptimisticResult();
			});
			state.value = getOptimisticResult();
		}
	}, { immediate: true });
	(0, vue_demi.watch)(defaultedQueries, (queriesValue) => {
		observer.setQueries(queriesValue, options);
		state.value = getOptimisticResult();
	});
	(0, vue_demi.onScopeDispose)(() => {
		unsubscribe();
	});
	return options.shallow ? (0, vue_demi.shallowReadonly)(state) : (0, vue_demi.readonly)(state);
}
//#endregion
exports.useQueries = useQueries;

//# sourceMappingURL=useQueries.cjs.map