Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils = require("./utils.cjs");
let vue_demi = require("vue-demi");
//#region src/useQueryClient.ts
function useQueryClient(id = "") {
	if (!(0, vue_demi.hasInjectionContext)()) throw new Error("vue-query hooks can only be used inside setup() function or functions that support injection context.");
	const key = require_utils.getClientKey(id);
	const queryClient = (0, vue_demi.inject)(key);
	if (!queryClient) throw new Error("No 'queryClient' found in Vue context, use 'VueQueryPlugin' to properly initialize the library.");
	return queryClient;
}
//#endregion
exports.useQueryClient = useQueryClient;

//# sourceMappingURL=useQueryClient.cjs.map